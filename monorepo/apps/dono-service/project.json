{
    "root": "dono-service",
    "projectType": "application",
    "sourceRoot": "apps/dono-service/src",
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "apps/dono-service",
          "color": true,
          "command": "sls package --stage=development --verbose"
        }
      },
      "offline": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "apps/dono-service",
          "color": true,
          "command": "sls offline --noAuth --stage=development --verbose"
        }
      },
      "deploy": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "apps/dono-service",
          "color": true,
          "command": "sls deploy --stage=prod --verbose"
        },
        "dependsOn": [
          {
            "target": "deploy",
            "projects": "dependencies"
          }
        ]
      }
    },
    "tags": ["service"],
    "implicitDependencies": []
  }